struct conf_struct {
	char name = calloc(/* length */, /* typesize */);
	/* other lines of meaningful config data */
	/* maybe function pointers for "object methods" */
} conf;

/* struct conf **net_list = <something goes here>; */
